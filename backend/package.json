{
  "name": "cafe-pos-demo-backend",
  "version": "1.0.0",
  "description": "餐厅 POS 系统后端 — Express + Socket.io + SQLite",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "node --watch server.js"
  },
  "dependencies": {
    "better-sqlite3": "^11.7.0",
    "express": "^4.21.0",
    "install": "^0.13.0",
    "socket.io": "^4.8.0"
  },
  "engines": {
    "node": ">=18.0.0 <26"
  }
}
